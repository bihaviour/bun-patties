---
title: AI and Agents
description: Agents, tools, jobs, and generated AGENTS.md metadata.
---

Patties includes optional AI primitives for projects that need agents, tools,
jobs, and generated `AGENTS.md` metadata.

The public AI surface includes:

- agent and tool config types
- `generateAgentsMd`
- AI runtime helpers under `patties/ai`

The current repository fixtures show the intended layout:

- `app/agents/` for agent definitions
- `app/tools/` for callable tools
- `app/jobs/` for scheduled jobs

During `patties build` and `patties dev`, Patties attempts to regenerate the
root `AGENTS.md` file from the app definition so downstream tools can inspect
routes, agents, tools, jobs, and middleware.
